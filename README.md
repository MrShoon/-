🤖 Telegram Stars Bot - Полное руководство

📋 ОПИСАНИЕ БОТА

Telegram Stars Bot - это бот для продажи Telegram Stars через два удобных способа оплаты. Бот предоставляет простой интерфейс для покупки Stars и полное управление для администраторов.

🎯 Основные функции:

Для пользователей:

· ⭐️ Покупка Telegram Stars - выбор из 6 пакетов (100-5000 Stars)
· 💳 Два способа оплаты:
  1. ЮKassa - безопасная оплата банковскими картами
  2. Перевод на карту - ручной перевод с контактом администратора
· 📋 История заказов - просмотр статуса всех покупок
· 💳 Информация об оплате - подробные инструкции по каждому способу

Для администраторов:

· 👑 Админ-панель - полный контроль над заказами
· 📊 Статистика - аналитика продаж и выручки
· 🔄 Управление ожидающими заказами - подтверждение оплат
· 📈 Просмотр всех заказов - мониторинг всей активности
· 🎯 Подтверждение выплат - ручное подтверждение получения платежей

🏗️ АРХИТЕКТУРА БОТА

Технологический стек:

· Python 3.8+
· Aiogram 3.x - асинхронный фреймворк для Telegram
· SQLite3 - легковесная база данных
· Aiohttp - асинхронные HTTP-запросы к ЮKassa API
· ЮKassa API - обработка платежей банковскими картами

База данных:

· users - информация о пользователях
· purchases - история всех покупок
· Колонки в purchases:
  · payment_method: yookassa или card_transfer
  · status: waiting_payment, paid, completed, cancelled
  · payment_id - ID платежа в ЮKassa
  · amount_rub - сумма в рублях

🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ

Шаг 1: Установка Python

1. Скачайте Python 3.8+ с официального сайта
2. Установите Python, отметьте галочку "Add Python to PATH"
3. Проверьте установку:

python --version
Шаг 2: Клонирование проекта

# Создайте папку для проекта
mkdir telegram_stars_bot
cd telegram_stars_bot

# Создайте файл main.py и вставьте в него код бота
Шаг 3: Установка зависимостей

Создайте файл requirements.txt:

aiogram==3.13.1
aiohttp==3.9.1
Установите зависимости:

pip install aiogram==3.13.1 aiohttp==3.9.1
Шаг 4: Настройка бота

4.1 Получите токен бота:

1. Откройте Telegram
2. Найдите бота @BotFather
3. Отправьте команду /newbot
4. Укажите имя бота (например: Stars Shop Bot)
5. Укажите юзернейм (например: stars_shop_bot)
6. Скопируйте токен (формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz)

4.2 Настройте ЮKassa:

1. Зарегистрируйтесь на ЮKassa
2. Создайте магазин в личном кабинете
3. Получите Shop ID и Secret Key
4. Включите тестовый режим для разработки

4.3 Настройте администраторов:

1. Узнайте свой Telegram ID через бота @userinfobot
2. Добавьте ID в список ADMIN_IDS в коде

Шаг 5: Конфигурация кода

В файле main.py замените следующие значения:

# Вставьте ваш токен от @BotFather
API_TOKEN = 'ВАШ_ТЕЛЕГРАМ_ТОКЕН'

# ID администраторов (можно несколько через запятую)
ADMIN_IDS = [123456789, 987654321]

# Данные ЮKassa
YOOKASSA_SHOP_ID = 'ВАШ_SHOP_ID_ЮКАССА'
YOOKASSA_SECRET_KEY = 'ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_ЮКАССА'
Шаг 6: Запуск бота

Вариант 1: Запуск напрямую

python main.py
Вариант 2: Запуск в фоновом режиме (Linux/Mac)

nohup python main.py > bot.log 2>&1 &
Вариант 3: Создание ярлыка (Windows)

1. Создайте файл start_bot.bat:

@echo off
python main.py
pause
1. Запустите двойным кликом

Шаг 7: Тестирование

Тестовые карты для ЮKassa:

Номер карты: 5555 5555 5555 4477
Срок: 01/30
CVV: 123
Проверка функционала:

1. Отправьте боту команду /start
2. Проверьте все кнопки меню
3. Создайте тестовый заказ
4. Протестируйте оба способа оплаты
5. Проверьте админ-панель (если ваш ID в ADMIN_IDS)

⚙️ КОНФИГУРАЦИЯ ПРОДАКШЕН

1. Настройка вебхука (рекомендуется для продакшена):

# Добавьте в код перед запуском
WEBHOOK_URL = "https://ваш-домен.com/webhook"
WEBHOOK_PATH = f"/webhook/{API_TOKEN}"

async def on_startup(dp):
    await bot.set_webhook(WEBHOOK_URL)

async def on_shutdown(dp):
    await bot.delete_webhook()
2. Использование PostgreSQL вместо SQLite:

import asyncpg

# Замените SQLite на PostgreSQL
DATABASE_URL = "postgresql://user:password@localhost/stars_bot"

3. Настройка логирования:

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('bot.log', encoding='utf-8'),
        logging.StreamHandler(),
        logging.handlers.RotatingFileHandler(
            'bot.log', maxBytes=10485760, backupCount=5
        )
    ]
)
🔧 РЕШЕНИЕ ПРОБЛЕМ

Частые ошибки и решения:

1. Ошибка: "Cannot install aiogram"

# Обновите pip
pip install --upgrade pip

# Установите с флагом --no-deps
pip install aiogram==3.13.1 --no-deps
pip install aiohttp==3.9.1
1. Ошибка: "SQLite database is locked"

# Увеличьте timeout в подключении
conn = sqlite3.connect('telegram_stars.db', timeout=30)
1. Ошибка: "TelegramNetworkError"

# Увеличьте таймауты
TIMEOUT = aiohttp.ClientTimeout(total=60, connect=15, sock_read=30)
1. Бот не отвечает на команды

· Проверьте токен бота
· Убедитесь, что бот не заблокирован
· Проверьте интернет-соединение

📊 АДМИНИСТРИРОВАНИЕ

Команды для админов:

· /start - главное меню
· 👑 Админ-панель - доступ к управлению
· 📊 Статистика - просмотр аналитики
· 🔄 Ожидают выдачи - заказы, требующие внимания
· 📈 Все заказы - полная история
· /complete_{id} - подтвердить выдачу Stars

Ручное управление БД:

# Просмотр базы данных
sqlite3 telegram_stars.db

# Просмотр пользователей
SELECT * FROM users;

# Просмотр заказов
SELECT id, user_id, stars_count, amount_rub, status FROM purchases;

# Экспорт данных
sqlite3 telegram_stars.db ".dump" > backup.sql
🔒 БЕЗОПАСНОСТЬ

Рекомендации:

1. Никогда не коммитьте токены и ключи в Git
2. Используйте переменные окружения:

import os
API_TOKEN = os.getenv('BOT_TOKEN')
YOOKASSA_SHOP_ID = os.getenv('YOOKASSA_SHOP_ID')
1. Регулярно делайте бэкапы базы данных
2. Ограничьте доступ к админ-панели только доверенным лицам
3. Используйте HTTPS для вебхуков

📈 МОНИТОРИНГ

Полезные метрики:

1. Количество пользователей - рост базы
2. Конверсия - соотношение просмотров/покупок
3. Средний чек - сумма покупки
4. Популярные пакеты - какие Stars лучше продаются
5. Способы оплаты - предпочтения пользователей

Логирование:

# Просмотр логов в реальном времени
tail -f bot.log

# Поиск ошибок
grep -i "error" bot.log

# Мониторинг активности
grep -i "покупка" bot.log | tail -20
🆘 ПОДДЕРЖКА

Если возникли проблемы:

1. Проверьте файл bot.log
2. Убедитесь в правильности токенов
3. Проверьте подключение к интернету
4. Для вопросов по ЮKassa: support@yookassa.ru
5. Для вопросов по Telegram API: @BotSupport

📞 КОНТАКТЫ

· Telegram для поддержки: @uraganprimee
· API ЮKassa: yookassa.ru/developers

---

Успешных продаж! 🚀
